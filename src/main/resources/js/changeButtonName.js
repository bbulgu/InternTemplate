//This is how you get an element with CSS selector, do some js to change text

AJS.toInit(function(){
    AJS.$("a.new-search.aui-button").text("newer search")
    AJS.$("#create_link").text("bulut's create")
});

